AI usage:

AI assistance was used to research safe handling of API responses that may return empty bodies (e.g. DELETE requests returning HTTP 204).
The solution was implemented and adapted to match the existing API client structure.

Explain optional chaining and give examples